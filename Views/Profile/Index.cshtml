@model DOTNET_PROJECT.Models.UserProfile

<h2>User Profile</h2>

<div>
    <dl>
        <dt>Email:</dt>
        <dd>@Model.Email</dd>
        <!-- Add other properties as needed -->
        <dt>Full Name:</dt>
        <dd>@Model.FullName</dd>
    </dl>
</div>
